<h1><%= current_user.first_name %>'s Favourite Posts</h1>

<% @favourites.each do |favourite| %>
  <div>
    <%= link_to favourite.post_title, post_path(favourite.post) %> by <%= favourite.post_author %>
  </div>
  <hr>
<% end %>
